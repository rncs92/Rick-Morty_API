<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Rick&Morty</title>
</head>
<body>
<div class="bg-cyan-700">
    <header>
        <div class="2xl:container 2xl:mx-auto">
            <div class="bg-cyan-700 dark:bg-gray-800 rounded shadow-lg py-5 px-7">
                <nav class="flex justify-between">
                    <div class="flex items-center space-x-3 lg:pr-16 pr-6">
                        <img class="cursor-pointer dark:bg-white p-1 rounded-full"
                             src="https://tuk-cdn.s3.amazonaws.com/can-uploader/header-1-svg1.svg" alt="circle"/>
                        <h2 class="font-normal text-3xl leading-6 text-gray-800 dark:text-white">Rick & Morty
                            Encyclopedia</h2>
                    </div>
                    <div class="flex justify-center items-center">
                        <form action="/" method="GET">
                            <input type="hidden" name="page" value="{{ home }}">
                            <button class="py-2.5 px-5 mr-2 mb-2 text-sm font-medium text-gray-900 focus:outline-none bg-white rounded-lg border border-gray-200 hover:bg-gray-100 hover:text-blue-700 focus:z-10 focus:ring-4 focus:ring-gray-200 dark:focus:ring-gray-700 dark:bg-gray-800 dark:text-gray-400 dark:border-gray-600 dark:hover:text-white dark:hover:bg-gray-700"
                                    type="submit">
                                Characters
                            </button>
                        </form>
                        <form action="/episodes" method="GET">
                            <input type="hidden" name="page" value="{{ home }}">
                            <button class="py-2.5 px-5 mr-2 mb-2 text-sm font-medium text-gray-900 focus:outline-none bg-white rounded-lg border border-gray-200 hover:bg-gray-100 hover:text-blue-700 focus:z-10 focus:ring-4 focus:ring-gray-200 dark:focus:ring-gray-700 dark:bg-gray-800 dark:text-gray-400 dark:border-gray-600 dark:hover:text-white dark:hover:bg-gray-700"
                                    type="submit">
                                Episodes
                            </button>
                        </form>
                        <form action="/locations" method="GET">
                            <input type="hidden" name="page" value="{{ home }}">
                            <button class="py-2.5 px-5 mr-2 mb-2 text-sm font-medium text-gray-900 focus:outline-none bg-white rounded-lg border border-gray-200 hover:bg-gray-100 hover:text-blue-700 focus:z-10 focus:ring-4 focus:ring-gray-200 dark:focus:ring-gray-700 dark:bg-gray-800 dark:text-gray-400 dark:border-gray-600 dark:hover:text-white dark:hover:bg-gray-700"
                                    type="submit">
                                Locations
                            </button>
                        </form>
                    </div>
                </nav>
                <br>
                <form action="/search" method="GET">
                    <label for="search" class="mb-2 text-sm font-medium text-gray-900 sr-only dark:text-white">Search</label>
                    <div class="relative">
                        <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                            <svg aria-hidden="true" class="w-5 h-5 text-gray-500 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                        </div>
                        <input type="search" id="search" name="name" class="block w-full p-4 pl-10 text-sm text-gray-900 border border-gray-300 rounded-lg bg-gray-50 focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500" placeholder="Search Character" required>
                        <button type="submit" class="text-white absolute right-2.5 bottom-2.5 bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">Search</button>
                    </div>
                </form>
                <br>
                <div class="grid grid-cols-4 gap-2">
                    {% for character in characters %}
                        <div class="max-w-sm bg-amber-200 border border-zinc-950 rounded-lg shadow dark:bg-gray-800 dark:border-gray-700">
                            <img class="rounded-t-lg" src="{{ character.avatar }}" alt=""
                                 style="display: block; margin: 0 auto;"/>
                            <div class="p-5">
                                <h3 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white">{{ character.name }}</h3>
                                <p class="mb-1 font-normal text-gray-800 dark:text-gray-400">
                                    <strong>Status: {% if character.status == 'Dead' %}
                                            <span class="font-bold text-red-900 dark:text-gray-400">{{ character.status }}</span>
                                        {% endif %}
                                        {% if character.status == 'Alive' %}
                                            <span class="font-bold text-green-900 dark:text-gray-400">{{ character.status }}</span>
                                        {% endif %}
                                        {% if character.status == 'unknown' %}
                                            <span class="font-bold text-gray-700 dark:text-gray-400">{{ character.status }}</span>
                                        {% endif %}
                                    </strong>
                                </p>
                                <p class="mb-1 font-normal text-gray-800 dark:text-gray-400">
                                    <strong>Species: </strong> {{ character.species }}</p>
                                <p class="mb-1 font-normal text-gray-800 dark:text-gray-400"><strong>Last known
                                        location: </strong> {{ character.location }}</p>
                                <p class="mb-1 font-normal text-gray-800 dark:text-gray-400"><strong>First seen
                                        in: </strong>{{ character.firstepisode.name }}</p>
                            </div>
                        </div>
                    {% endfor %}
                </div>
                <div class="flex justify-center items-center">
                    <form action="/test" method="GET">
                        <input type="hidden" name="page" value="{{ previous_page }}">
                        <button class="bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded-l-lg"
                                type="submit">
                            Previous
                        </button>
                    </form>
                    <form action="/test" method="GET">
                        <input type="hidden" name="page" value="{{ next_page }}">
                        <button class="bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded-r-lg"
                                type="submit">
                            Next
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </header>
</div>
</body>
<footer
        class="bg-neutral-200 text-center dark:bg-neutral-700 lg:text-left">
    <div class="p-4 text-center text-neutral-700 dark:text-neutral-200">
        © 2023 Copyright:
        <a
                class="text-neutral-800 dark:text-neutral-400"
                href="https://instagram.com/skepausis/"
        >Jānis Šķēps</a
        >
    </div>
</footer>
</html>